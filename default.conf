server {
    listen 80;
    root /var/www/personal_system;
    index index.php;
    access_log /var/log/nginx/personal_system_access.log;
    error_log /var/log/nginx/personal_system_error.log;
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    location ~ \.php$ {
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }
}